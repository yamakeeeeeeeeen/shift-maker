const inputDir = "./csv";
export const INPUT_PATH = {
  CAREGIVER: `${inputDir}/caregiver.csv`,
  USER: `${inputDir}/user.csv`,
};

export const OUTPUT_DIR = "./output";
export const OUTPUT_PATH = `${OUTPUT_DIR}/shift.csv`;
